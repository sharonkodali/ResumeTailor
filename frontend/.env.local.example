# Base URL of the FastAPI backend (backend/ runs on 8000 by default).
# Copy this file to .env.local. Next inlines NEXT_PUBLIC_* at build time,
# so set it before `npm run build`, not before `npm start`.
NEXT_PUBLIC_API_URL=http://localhost:8000
